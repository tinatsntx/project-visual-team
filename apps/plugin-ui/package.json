{
  "name": "@visual-team/plugin-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.tsx --bundle --format=esm --jsx=automatic --outfile=dist/visual-team.js --minify && esbuild src/dev/devHost.ts --bundle --format=esm --outfile=dist/dev-host.js --minify",
    "dev": "esbuild src/main.tsx --bundle --format=esm --jsx=automatic --outfile=dist/visual-team.js --watch",
    "dev:serve": "node serve.mjs"
  },
  "dependencies": {
    "@visual-team/contracts": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@visual-team/codex-event-mapper": "*",
    "@visual-team/state-machine": "*",
    "@visual-team/test-fixtures": "*"
  }
}
